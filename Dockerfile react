FROM node:latest

WORKDIR /app

COPY package*.json ./
RUN npm install

COPY . .

EXPOSE 5173

CMD ["npm", "run", "dev", "--", "--host"]



# cd C:/Users/jijan/OneDrive/Desktop/Amit/react-vite-docker   //Navigate to your project folder
# docker run -it --rm -v "C:/Users/jijan/OneDrive/Desktop/Amit/react-vite-docker:/app" -w /app node:latest bash  //Run a temporary Node.js container to create your React Vite app
# Inside the container, create the React Vite app
# now past vite config for hot reloading.
# docker build -t react-vite-app .  //Create a Docker image for your app
# docker run -it --rm -p 5173:5173 -v "C:/Users/jijan/OneDrive/Desktop/Amit/react-vite-docker:/app" -w /app react-vite-app    //Run the Docker container with port and volume mappings     



import { defineConfig } from 'vite'
import react from '@vitejs/plugin-react'

export default defineConfig({
  plugins: [react()],
  server: {
    watch: {
      usePolling: true
    },
    host: true,
    port: 5173
  }
})

    
